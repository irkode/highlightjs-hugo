# The full vocabulary for plain Go templates. hugoDocs documents Hugo, not Go,
# so nothing is scraped for this language -- these lists are the whole input.
fixed:
  BUILTINS:
    - urlquery
    - println
    - printf
    - index
    - print
    - slice
    - call
    - html
    - and
    - len
    - not
    - eq
    - ge
    - gt
    - js
    - le
    - lt
    - ne
    - or
  LITERALS:
    - "false"
    - nil
    - "true"

patterns:
  KEYWORDS_PIPELINE:
    - block
    - define
    - else\s+with
    - else\s+if
    - range
    - return
    - template
    - with
    - if
  KEYWORDS_STANDALONE:
    - break
    - continue
    - else
    - end

generate:
  LITERALS: ['kw']
  BUILTINS: ['kw']
  FUNCTIONS: []
  KEYWORDS:  []
